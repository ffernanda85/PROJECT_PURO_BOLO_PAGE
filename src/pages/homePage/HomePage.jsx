import { Header } from '../../components/header/Header'
import * as s from './styleHomePage'

export const HomePage = () => {
    return (
        <s.HomePageContainer>
            <Header/>
            
        </s.HomePageContainer>
    )
}